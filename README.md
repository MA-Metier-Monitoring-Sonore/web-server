# Web Server
This repository contains the Docker structure for the web server (`frontend`, `backend`, `database`)

## Prerequisites

To make this structure run, first copy the `.env.example` to `.env` and fill in the values.

## Deploy

To run the containers, run this command from your terminal :
```bash
docker compose up -d --build
```
